## What's here

- **Loyalty points** — award/redeem logic (`docs/loyalty-points.md`).
- **Kiosk offline queue** — buffers orders to disk when the kiosk loses
  network, flushes on reconnect (`docs/offline-queue.md`).
- **Receipt templates** — reworked receipt rendering to use Jinja2
  (`docs/receipt-templates.md`).
- **CI** — add a lint-and-test workflow that runs on every PR.
