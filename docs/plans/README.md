# Plans

One-time procedures tracked to completion, then **retired** (the file is deleted once its work is done and the retirement is confirmed).
The permanent record of what a plan produced lives in ADRs and the plugins themselves, not here.

| Plan | Status |
|---|---|
| [Marketplace build-out](marketplace-buildout.md) | In progress — structure decided (ADR-001, ADR-002); split (#1), bootstrap + git-conventions dogfood (#2), `docs-standards` (#4), `claude-code-web` (#5), `upstream-research` (#6), `terraform-standards` (#7), `terraform-provider-standards` (#8), `scaffolding` (#10), and `markdown-standards` (#14) merged; only this repo's own CI remains |
