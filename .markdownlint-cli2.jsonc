// markdownlint rules for this repo, consumed by the markdown-lint workflow
// (flungo/github-workflows). Standard defaults per that repo's
// docs/reference/markdown-validation.md; add further overrides only with an
// inline justification.
{
  "config": {
    // Disabled in favour of the semantic line breaks convention (one sentence
    // per source line) — a character ceiling can't reflow and would fight it.
    "MD013": false,
    // Allow repeated subsection names under different parents (e.g. Context /
    // Decision / Consequences across ADRs); paired with the unique-heading
    // convention for anything cross-referenced.
    "MD024": { "siblings_only": true },
    // Pin the compact table style rather than leaving MD060 to infer it. The
    // default "consistent" infers per table and picks "aligned" — every cell
    // padded to column width — for tables no row disambiguates, which --fix
    // will not produce and which is not the style the conventions record.
    "MD060": { "style": "compact" }
  },
  // Eval fixtures are dev-time artifacts reproduced verbatim (e.g. PR
  // descriptions, deliberately messy inputs) — linting them would alter what
  // they exist to preserve. Plugin docs outside fixtures/ are still linted.
  "ignores": ["plugins/*/evals/fixtures/**"]
}
